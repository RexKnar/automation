/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.pravatar.cc', 'storage.googleapis.com', 'rexcoders.online', 'process.fs.teachablecdn.com'],
    },
};

export default nextConfig;
